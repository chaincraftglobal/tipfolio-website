{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "//": "The site is generated into docs/ (a GitHub Pages convention). Vercel serves the repo root by default, which holds no index.html - that is what produces NOT_FOUND. This points it at the right directory.",
  "buildCommand": null,
  "installCommand": null,
  "outputDirectory": "docs",

  "//trailingSlash": "Load-bearing. The iPhone app ships https://tipfolio.app/terms/ and /privacy/ WITH trailing slashes in LegalLinks, and every canonical URL on the site matches. Without this, /terms would 404 or redirect through a non-canonical URL that App Review follows.",
  "trailingSlash": true,

  "cleanUrls": false,

  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    },
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        { "key": "X-Frame-Options", "value": "SAMEORIGIN" }
      ]
    }
  ]
}
